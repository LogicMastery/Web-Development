var something = "Write something"

if(something == something){
    console.log(something)
    var something2 = "Do you see me?"
}

console.log(something2)

    // Let - it can only be seen inside the curly braces { }
    if(something2 == something2){
        let WriteAnything = "I'm right over here"
        console.log(WriteAnything)
    }

    // console.log(WriteAnything)

    // Const - cannot be overwrite, and it can only be seen inside the curly braces { }
    if(something2 == something2){
        const OverWriteMe = "I'm right over here"
        // OverWriteMe = "Can I be changed?"
        console.log(OverWriteMe)
    }